#!/usr/bin/env groovy

// Name is just for info purposes
def call(name, packageVersionId, packagePassword) {

    echo "Install ${name} package"

    shWithStatus "sfdx force:package:install --package ${packageVersionId} --installationkey ${packagePassword} --wait 15 --noprompt"
    
    return this
}
